import Nav from '@/components/Nav'
import React from 'react'

const layout = (props) => {
    const {children} = props
  return (
    <div>
        {children}
        
    </div>
  )
}

export default layout